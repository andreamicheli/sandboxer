# Reset Sandboxer on the real pilot

The active Sandboxer tree starts from the Inspect/Groq real-model pilot and its later iterations. The earlier synthetic `cyberrumble` implementation is deprecated and removed from the current branch; Git history remains the archive, avoiding a second repository while preventing the discarded approach from shaping the new architecture.
